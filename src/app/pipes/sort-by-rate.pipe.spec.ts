import { SortByRatePipe } from './sort-by-rate.pipe';

describe('SortByRatePipe', () => {
  it('create an instance', () => {
    const pipe = new SortByRatePipe();
    expect(pipe).toBeTruthy();
  });
});
